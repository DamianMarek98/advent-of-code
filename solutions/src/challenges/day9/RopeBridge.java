package challenges.day9;

import challenges.FileLineIterator;
import challenges.Solver;

public class RopeBridge implements Solver<Integer> {

    @Override
    public Integer solve() {
        for (var line : new FileLineIterator(9, false)) {
            var a = "";
        }

        return null;
    }
}
